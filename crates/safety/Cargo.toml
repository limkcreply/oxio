[package]
name = "safety"
description = "oxio safety rails: permission gate for write/exec tools (composing Tool wrapper) + a cheap deterministic loop guard. No model-call cost."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
oxio-core = { path = "../oxio-core" }
async-trait.workspace = true
serde_json.workspace = true

[dev-dependencies]
tokio.workspace = true
