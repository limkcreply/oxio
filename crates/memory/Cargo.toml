[package]
name = "memory"
description = "Durable cross-session memory: a file-backed store and a PreTurn injector that feeds stored facts back into context, with extraction and consolidation."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
oxio-core = { path = "../oxio-core" }
async-trait.workspace = true
dirs.workspace = true  # locate the config dir for the user-global memory store (beside config.toml)

[dev-dependencies]
tokio.workspace = true
